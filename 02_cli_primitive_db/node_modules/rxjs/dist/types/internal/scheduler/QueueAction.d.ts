import { AsyncAction } from 'rxjs/dist/types/internal/scheduler/AsyncAction';
import { Subscription } from 'rxjs/dist/types/internal/Subscription';
import { QueueScheduler } from 'rxjs/dist/types/internal/scheduler/QueueScheduler';
import { SchedulerAction } from 'rxjs/dist/types/internal/types';
import { TimerHandle } from 'rxjs/dist/types/internal/scheduler/timerHandle';
export declare class QueueAction<T> extends AsyncAction<T> {
    protected scheduler: QueueScheduler;
    protected work: (this: SchedulerAction<T>, state?: T) => void;
    constructor(scheduler: QueueScheduler, work: (this: SchedulerAction<T>, state?: T) => void);
    schedule(state?: T, delay?: number): Subscription;
    execute(state: T, delay: number): any;
    protected requestAsyncId(scheduler: QueueScheduler, id?: TimerHandle, delay?: number): TimerHandle;
}
//# sourceMappingURL=QueueAction.d.ts.map