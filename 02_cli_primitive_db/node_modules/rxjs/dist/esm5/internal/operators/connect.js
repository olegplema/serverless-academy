import { Subject } from 'rxjs/dist/cjs/internal/Subject';
import { innerFrom } from 'rxjs/dist/cjs/internal/observable/innerFrom';
import { operate } from 'rxjs/dist/cjs/internal/util/lift';
import { fromSubscribable } from 'rxjs/dist/cjs/internal/observable/fromSubscribable';
var DEFAULT_CONFIG = {
    connector: function () { return new Subject(); },
};
export function connect(selector, config) {
    if (config === void 0) { config = DEFAULT_CONFIG; }
    var connector = config.connector;
    return operate(function (source, subscriber) {
        var subject = connector();
        innerFrom(selector(fromSubscribable(subject))).subscribe(subscriber);
        subscriber.add(source.subscribe(subject));
    });
}
//# sourceMappingURL=connect.js.map