import { mergeAll } from 'rxjs/dist/cjs/internal/operators/mergeAll';
import { innerFrom } from 'rxjs/dist/cjs/internal/observable/innerFrom';
import { EMPTY } from 'rxjs/dist/cjs/internal/observable/empty';
import { popNumber, popScheduler } from 'rxjs/dist/cjs/internal/util/args';
import { from } from 'rxjs/dist/cjs/internal/observable/from';
export function merge() {
    var args = [];
    for (var _i = 0; _i < arguments.length; _i++) {
        args[_i] = arguments[_i];
    }
    var scheduler = popScheduler(args);
    var concurrent = popNumber(args, Infinity);
    var sources = args;
    return !sources.length
        ?
            EMPTY
        : sources.length === 1
            ?
                innerFrom(sources[0])
            :
                mergeAll(concurrent)(from(sources, scheduler));
}
//# sourceMappingURL=merge.js.map